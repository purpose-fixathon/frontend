import { StyleSheet } from 'react-native'

const header = StyleSheet.create({
    title: {
        textAlign: 'center',
        fontSize: 18,
        fontWeight: 'bold'
    },
})

export default header